## الذاكرة — مستودع project-brain

هذا المستودع جزء من مشروع الزاحف. معرفة المشروع كاملة على فرع `insolvenz-scraper` في
`Swift-Assets/project-brain`: ما هو، كيف يعمل، الثوابت التي لا تُكسر، أين وصل، إلى أين
يتجه، ما ينقصه، والأخطاء التي وقعت فيه ولا تُكرَّر.

افتح الجلسة بمستودعين — هذا المستودع، و `project-brain` **على فرع `insolvenz-scraper`** —
واقرأ `README.md` هناك قبل لمس أي شيء. وقبل أن تنتهي، حدّث `README.md` وأضف فقرة إلى
`LOG.md` على ذلك الفرع.

## ثوابت هذا المستودع — لا تُكسر

1. **الميزانية في القاعدة، لا في العملية.** كل طلب يسأل
   `swift_v2.registry_claim_request` أولاً ويطيع الجواب. لا عدّاد محلي، ولا متغيّر بيئة
   يرفع السقف.
2. **الافتراضات آمنة بلا Whitelist.** قيد `registry_source_config_ceiling_ck` يرفض أي
   سقف فوق 55/ساعة و900/يوم ما لم يُسجَّل منح Whitelist، وفوق 150/ساعة و1000/يوم في كل حال.
3. **لا تاريخ ميلاد ولا عنوان سكن لشخص طبيعي، في أي مكان** — لا في عمود، ولا في ملف،
   ولا في سجلّ تشغيل. `xjustiz.redact_si` قائمة سماح: ما لا يُذكر فيها يُحذف.
4. **لا تشغيل إلا من الخادم ذي الـIP الثابت.** لا GitHub Actions، ولا Edge Function،
   ولا جهاز محمول. البوابة تعدّ الاستدعاءات لكل عنوان، وطلبنا يسمّي عنواناً واحداً.
5. **لا يُسمّى أي مخرَج للزبون «Handelsregister»** (§ 8 Abs. 2 HGB). اسم المجلّد شأن داخلي.

## قبل أي تغيير

اقرأ `docs/RUNBOOK.md`. الاختبارات تعمل بلا شبكة:
`python -m unittest discover -s tests -t .`
